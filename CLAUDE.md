# Galaxy — 우주 스케일 줌 웹앱

지구에서 시작해 스크롤로 점점 멀어지며(수성·금성·화성 → 태양계 → 우리은하 → 외부은하 → 은하군 → 관측 가능한 우주) 공간의 크기를 체감시키는 단일 페이지 웹앱.

## 핵심 아이디어

화면에 담기는 실제 폭(미터)을 **로그 스케일** 변수 하나로 잡는다.

```
zoom = log10(화면 가로 폭, 미터)   // 7 (≈1만 km, 지구) → 27 (≈10^27 m, 관측 우주)
```

스크롤/휠 = `zoom` 증감. 그게 전부다. 각 천체는 실제 크기(m)와 지구로부터의 거리(m)만 갖고 있고, 화면상 픽셀 크기는 `size_m / 10^zoom * 화면폭` 으로 매 프레임 계산한다.
0.5px 미만이면 안 그리고, 화면보다 크면 안 그린다. → 줌 단계별 "레이어" 관리 코드가 필요 없다.

## 지구는 항상 화면 중앙 (원점 고정)

모든 좌표가 **지구 기준 상대 위치**이므로, 지구를 캔버스 정중앙(0,0)에 고정하면 카메라 이동 코드 자체가 없어진다. 줌만 변한다.

이게 공짜로 주는 것:
- 아무리 멀어져도 중앙의 **"현재 위치" 레티클**(작은 십자선+원)이 계속 남는다. 지구가 0.5px 이하로 사라진 뒤에도 이 마커는 항상 그린다 — 스케일 감각의 기준점.
- 우리은하를 그릴 때 **은하 중심이 화면 중앙이 아니라 한쪽으로 치우쳐** 보인다. 태양계는 은하 중심에서 약 2.6만 광년 떨어진 오리온 팔에 있으니까. 별도 처리 없이 데이터가 상대 좌표라서 자동으로 맞는 그림이 된다. 안드로메다 방향, 국부은하군 안에서의 위치도 마찬가지.
- 상단에 **빵부스러기**: 지금 시점에서 "나를 담고 있는" 가장 작은 구조를 표시.
  `지구 › 태양계 › 오리온 팔 › 우리은하 › 국부은하군 › 라니아케아 › 관측 가능한 우주`
  구현은 한 줄 — `radius_m > 현재 화면폭` 인 천체 중 가장 작은 것.

## 기술 선택

- **바닐라 HTML + Canvas 2D, 파일 1개(`index.html`)**. 빌드 없음, 의존성 없음, 서버 없음. 더블클릭하면 열린다.
- three.js / React / 번들러 전부 생략 — 원·라벨·로그축만 그리면 되므로 2D로 충분.
- 배포는 GitHub Pages에 파일 하나 올리면 끝.

## 데이터 (하드코딩 배열 하나)

```js
// { name, radius_m, dist_m, angle, color }
// dist_m = 지구로부터의 거리, angle = 지구 기준 방향(라디안)
// 지구 자신은 dist_m: 0 → 항상 중앙
```

`angle`은 실제 천구 좌표를 쓸 필요 없다. 은하 중심·안드로메다처럼 **방향이 의미 있는 것만** 대략 맞춰주고, 나머지는 겹치지 않게 적당히 흩뿌린다. (자릿수가 중요하지 정밀 성도가 목적이 아님)

대략 20~30개면 충분:

| 구간 | 예시 | zoom 범위 |
|---|---|---|
| 지구 근방 | 지구, 달, 지구-달 거리 | 7–9 |
| 내행성 | 수성·금성·화성, 태양, 1 AU | 9–12 |
| 외행성 | 목성·토성·천왕성·해왕성, 카이퍼벨트 | 12–14 |
| 항성 간 | 오르트 구름, 프록시마 켄타우리, 1 광년 | 15–17 |
| 우리은하 | 오리온 팔, 은하 중심, 은하 원반 10만 광년 | 18–21 |
| 국부은하군 | 안드로메다, 마젤란운, 국부은하군 300만 광년 | 22–23 |
| 그 너머 | 처녀자리 초은하단, 라니아케아, 우주 거대구조, 관측 가능한 우주 | 24–27 |

숫자 출처는 NASA/위키 값 그대로. 정확도보다 자릿수가 중요.

## 화면 구성

- **중앙 = 지구/태양계, 항상 고정.** 사라지지 않는 "You are here" 레티클 + 현재 구조 이름
- 상단: 빵부스러기 (지구 › 태양계 › … › 관측 가능한 우주), 현재 위치 하이라이트
- 좌하단: 현재 스케일 표시 (`10^21 m · 약 10만 광년`) — km/AU/광년/Mpc 중 자동 선택
- 우측: 세로 눈금 슬라이더(현재 위치 표시 + 드래그로 점프)
- 화면 밖으로 나가는 천체 이름을 페이드 인/아웃으로 라벨링

## 구현 순서

1. `index.html` 뼈대 + canvas + 휠/터치 → `zoom` 값 연결, 숫자만 화면에 출력
2. 데이터 배열 + 원 렌더링 (크기 필터링 포함)
3. 단위 자동 변환 함수 (`formatDistance(m)`) — 여기만 테스트 1개 (`m/km/AU/ly/Mpc` 경계값 assert)
4. 중앙 레티클 + 빵부스러기 (가장 작은 "나를 담는 구조" 찾기)
5. 라벨 + 페이드
6. 눈금 슬라이더, 키보드(↑↓), 모바일 핀치
7. (선택) 배경 별, 은하 이미지 스프라이트

## 일부러 안 하는 것

- 3D/궤도 역학/실시간 행성 위치 → 스케일 체감이 목적, 천문 시뮬레이터가 아님
- 이미지 에셋 → 처음엔 색 원으로. 나중에 필요하면 그때 추가
- 프레임워크/상태관리 → 상태가 `zoom` 변수 하나뿐
- 팬(pan)/카메라 이동 → 지구가 원점 고정이라 필요 없음. 좌우로 끌고 다니고 싶어지면 그때 추가
