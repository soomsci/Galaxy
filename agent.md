# Galaxy — 3D 우주 스케일 줌 웹앱 작업 계획

## 1. 목표와 현재 상태

지구에서 출발해 달, 태양계, 항성 주변, 우리은하, 국부은하군, 우주 거대구조, 관측 가능한 우주까지 확대·축소하며 공간의 크기를 체감하는 단일 페이지 웹앱을 만든다.

현재 저장소에는 `CLAUDE.md`의 초기 기획만 있으며 구현 코드는 없다. 이 문서는 기존 기획을 바탕으로 예상되는 3D 작업을 반영한 후속 계획이다. 기존 문서는 보존한다.

핵심 경험은 다음과 같다.

- 지구를 관측 기준점으로 유지하고 중앙에 현재 위치 마커를 표시한다.
- 휠, 터치, 키보드, 슬라이더로 우주 규모를 연속적으로 탐색한다.
- 천체의 크기와 거리, 화면을 채우는 구조가 어떻게 달라지는지 이해할 수 있게 한다.
- 실제 축척과 가독성을 위한 시각적 보조 표현을 명확히 구분한다.

## 2. 기존 계획에서 바꿀 사항

| 기존 계획 | 수정 방향 | 이유 |
|---|---|---|
| Canvas 2D와 단일 HTML | Three.js 기반 3D 렌더러와 모듈 구조 | 구체, 조명, 은하 입자, 장면 전환을 독립적으로 관리 |
| 빌드와 의존성 없음 | Vite 개발 서버 및 정적 빌드 | 모듈과 텍스처 로딩, 배포 경로 관리 |
| 모든 천체를 같은 방식으로 그림 | 규모별 표현과 상세 수준(LOD) 적용 | 행성 표면과 은하·거대구조는 표현 방법이 다름 |
| 미터 좌표를 그대로 화면에 투영 | 기준점 상대 좌표와 정규화된 렌더 좌표 | 광범위한 크기 차이에서 GPU 정밀도 손실 방지 |
| 반지름만 비교해 현재 구조 선택 | 명시적 포함 관계와 단계별 전환 구간 | 큰 천체라고 지구를 포함하는 구조는 아님 |
| 화면보다 큰 천체는 숨김 | 시야 교차 여부와 표현 단계로 판단 | 지구 근접 화면이나 화면을 가로지르는 은하가 갑자기 사라지는 문제 방지 |
| 3D 제외 | 3D를 기본으로 하되 범위를 단계적으로 확장 | 초기부터 전체 우주를 사실적인 모델로 만드는 작업 방지 |

React와 별도 상태관리 라이브러리는 초기 범위에 포함하지 않는다. 배포 결과는 정적 파일이며 GitHub Pages 배포를 기준으로 준비한다. HTML 더블클릭 실행 대신 개발 서버와 빌드 미리보기를 사용한다.

## 3. 스케일과 카메라 설계

### 공통 상태

```js
zoom = Math.log10(viewWidthMeters);
viewWidthMeters = 10 ** zoom;
```

초기 탐색 범위는 기존 기획의 `7–27`을 출발점으로 삼되, 지구 전체와 최종 우주 경계가 여백을 두고 보이도록 실제 데이터와 화면 비율에 맞춰 시작값·종료값을 조정한다. 화면 가로 폭, 천체 반지름, 천체 지름을 혼용하지 않는다.

- 입력은 `targetZoom`을 변경하고 화면은 `currentZoom`을 통해 부드럽게 따라간다.
- 프레임 시간에 기반해 보간하여 주사율에 따른 속도 차이를 줄인다.
- 애니메이션 축소 설정에서는 보간·자동 회전을 줄이거나 즉시 이동한다.
- 중앙 기준점은 지구이며 `zoom`과 별도로 품질 설정, 선택된 대상, 장면 전환 상태를 관리한다.

### 기본 투영

초기 구현은 직교 카메라를 사용한다. 구체와 조명은 3D로 표현하면서, 기준 평면의 화면 가로 폭과 축척을 직접 연결할 수 있다. 자유 이동과 사용자 카메라 회전은 초기 범위에서 제외하고, 단계에 적합한 고정 관측 방향을 사용한다.

렌더링 좌표계에서는 화면 가로 폭을 일정한 값 `W`로 두고 다음과 같이 변환한다.

```js
renderPosition = relativePositionMeters / viewWidthMeters * W;
renderRadius = radiusMeters / viewWidthMeters * W;
```

세로 시야는 화면 종횡비로 계산한다. 기준 평면에서 천체 지름의 픽셀 크기는 `2 * radiusMeters / viewWidthMeters * viewportWidthPx`다. 필요할 경우 향후 원근 카메라를 추가하되, 원근 투영에서는 기준 평면 밖의 물체에 이 공식을 그대로 적용하지 않는다.

### 정밀도와 깊이 처리

- 미터 단위 원본 데이터와 GPU에 전달하는 렌더 좌표를 분리한다.
- 서로 다른 규모를 하나의 거대한 절대 좌표 장면에 넣지 않는다.
- 태양계·은하 등 각 구조의 로컬 좌표와 기준점 변환을 관리하고, 큰 절대 좌표끼리 빼서 작은 거리를 복원하는 계산을 피한다.
- 활성 단계에 필요한 데이터만 로드·변환하며 정규화 이후에도 범위를 벗어나는 대상은 제외한다.
- 카메라의 near/far는 활성 장면의 깊이 범위에 맞춘다. 전체 우주를 하나의 깊이 범위로 처리하지 않는다.
- 초기 검증에서 깊이 충돌과 전환 깜빡임을 확인한 후 렌더 패스 분리 여부를 결정한다.

## 4. 3D 표현 전략

| 구간 | 기본 표현 | 후속 개선 |
|---|---|---|
| 지구·달 | 구체 메시, 간단한 재질, 방향광 | 표면 텍스처, 지구 구름·대기 |
| 태양계 | 실제 반지름을 가진 구체, 태양의 발광 표현, 거리 안내 | 토성 고리, 선택한 행성의 세부 재질 |
| 항성 간 공간 | 점·빌보드, 거리 기준선 | 밝기와 색상 차이 |
| 우리은하 | 기울어진 원반과 입자, 은하 중심에서 벗어난 태양계 위치 | 나선팔과 먼지의 절차적 표현 |
| 국부은하군 | 단순화한 은하 메시·스프라이트 | 주요 은하별 형태 차이 |
| 우주 거대구조 | 입자와 필라멘트의 개념적 시각화 | 출처가 있는 관측 데이터 적용 |
| 관측 가능한 우주 | 관측자 중심 경계와 거리 안내 | 경계의 의미를 설명하는 보조 정보 |

입자나 필라멘트를 절차적으로 생성하면 실제 관측 분포가 아닌 개념도임을 표시한다. 관측 가능한 우주의 경계를 물리적인 벽 또는 우주 전체의 끝처럼 표현하지 않는다.

### 장면 전환과 LOD

- `zoom`을 공통 축으로 두고 각 단계에 진입·퇴장 및 겹침 구간을 설정한다.
- 경계에서는 불투명도를 교차 전환하고 이전 구조의 위치가 다음 구조 안에서 이어지도록 기준점을 맞춘다.
- 전환 시 최대 두 단계만 활성화하는 것을 기본 정책으로 삼는다.
- 픽셀보다 작은 실제 천체 메시지는 숨길 수 있지만, 별도 크기의 위치 마커와 이름은 유지할 수 있다.
- 화면상의 최소 크기를 보장하는 마커는 실제 천체 크기를 나타내지 않는다고 안내한다. 실제 메시 크기를 몰래 부풀리지 않는다.
- 라벨에는 우선순위, 충돌 회피, 최대 표시 수, 전환 여유 구간을 적용한다.
- 중앙 현재 위치 마커는 HTML/SVG 오버레이로 구현해 깊이 가림과 축척의 영향을 받지 않게 한다.

## 5. 데이터와 정확성 기준

초기 콘텐츠는 기존 기획의 20~30개 주요 대상으로 시작한다. 먼저 지구·달·태양만으로 렌더링 구조를 검증한 뒤 확장한다.

데이터는 화면 표현과 분리하고 아래 정보를 저장한다.

```js
{
  id, name, kind,
  parentId,             // 소속 구조; 카메라 좌표 기준과 구분
  frameId,              // 로컬 좌표계 식별자
  radiusMeters,         // 해당 의미가 있는 대상만 사용
  positionMeters,       // 해당 로컬 좌표계의 [x, y, z]
  representation,       // sphere, points, galaxy 등
  scaleRange,           // 표현에 적합한 zoom 범위
  accuracy,             // observed, approximate, illustrative
  sources               // URL, 사용한 수치·정의, 확인일
}
```

- 거리와 크기는 NASA 등 신뢰할 수 있는 자료를 구현 시 확인하고 출처를 함께 남긴다.
- 행성의 지구와의 거리는 시간에 따라 달라진다. 기준 시점이 없는 정적 배치는 예시 배치라고 안내한다.
- 실제 천구 좌표를 쓰지 않는 대상은 방향을 정확한 관측 결과처럼 설명하지 않는다.
- 별과 행성의 반지름, 은하의 대표 지름, 구조의 범위 등 서로 다른 정의를 구분한다.
- 큰 규모의 거리에는 사용한 거리 정의를 기록하고 서로 다른 정의를 무심코 섞지 않는다.
- 단위 상수와 `m/km/AU/ly/pc/Mpc/Gpc` 변환은 공통 모듈에 둔다.
- 빵부스러기는 지구를 포함하는 구조의 명시적 경로와 현재 표현 단계를 기준으로 정한다. 크기 정렬만으로 포함 관계를 추론하지 않는다.
- 오르트 구름과 초은하단처럼 경계·소속의 정의가 모호한 경우 설명에 반영하고, 모든 구조를 엄밀한 중첩 껍질처럼 표시하지 않는다.

## 6. 화면과 조작

- 중앙: 지구 기준점 마커와 현재 규모의 기준 위치 설명.
- 상단: 지구에서 현재 구조까지 이어지는 탐색 경로.
- 좌하단: 화면 가로 폭, 자동 변환한 거리 단위, 눈금 막대.
- 우측: 로그 스케일 슬라이더와 주요 단계 이동 버튼.
- 보조 정보: 선택한 대상의 크기·거리·출처와 개념도 여부.
- 입력: 휠, 터치 핀치, 방향키, 접근 가능한 네이티브 슬라이더.
- 모든 입력은 동일한 줌 제어기를 사용하고 범위 밖의 값은 제한한다.
- 터치 제스처는 캔버스 안에서 처리하며 주변 UI 조작과 충돌하지 않게 한다.
- 설명과 조작 요소는 DOM으로 제공한다. 색상만으로 상태를 구분하지 않는다.
- WebGL 사용 불가 또는 컨텍스트 손실 시 상태 안내와 복구 경로를 제공한다. 사용 불가 환경에서도 주요 규모 설명은 읽을 수 있어야 한다.

## 7. 권장 파일 구조

```text
index.html
package.json
src/
  main.js
  styles.css
  core/            # 줌 상태, 단위 변환, 좌표 변환
  data/            # 천체 데이터, 구조 관계, 출처
  rendering/       # 렌더러, 카메라, 품질 설정, 자원 정리
  scenes/          # 행성, 태양계, 은하, 거대구조
  ui/              # 라벨, 경로, 눈금, 입력
public/
  assets/          # 텍스처 등 정적 에셋
tests/             # 핵심 수학·상태 검증
docs/
  sources.md       # 데이터와 에셋 출처·라이선스
  qa.md            # 기기별 검증 결과와 알려진 제한
```

실제 코드가 필요해질 때 파일을 추가한다. 계획만을 위해 빈 추상화 계층이나 다수의 빈 파일을 만들지 않는다.

## 8. 구현 순서와 완료 조건

### 1단계 — 3D 기술 검증

Vite와 Three.js를 구성하고 지구·달·태양, 직교 카메라, 줌 상태, 중앙 마커, 스케일 숫자를 구현한다. 초기에는 외부 텍스처 없이 기본 재질을 쓴다.

완료 조건: 세 천체 구간을 왕복할 때 지구 기준점이 유지되고, 거리와 지름의 투영이 계산값과 일치하며, 화면 크기 변경 시 왜곡이나 깊이 깜빡임이 없다.

### 2단계 — 전체 규모 연결

태양계, 항성 간 공간, 우리은하, 국부은하군, 거대구조, 관측 가능한 우주를 단순한 대체 형상으로 연결한다. 로컬 좌표 변환, 단계 활성화, LOD, 교차 전환을 구현한다.

완료 조건: 전체 범위를 연속 왕복할 수 있고 단계 경계에서 위치 기준이 튀거나 콘텐츠가 의도치 않게 모두 사라지지 않는다. 은하 중심과 태양계 위치를 구분할 수 있다.

### 3단계 — 탐색 가능한 MVP

20~30개 주요 대상과 출처, 단위 변환, 빵부스러기, 라벨, 슬라이더, 키보드, 모바일 핀치와 축척 안내를 완성한다.

완료 조건: 마우스·터치·키보드로 같은 범위를 탐색할 수 있고, 현재 화면 폭과 구조를 이해할 수 있으며, 실제 축척과 위치 마커를 구분할 수 있다.

### 4단계 — 3D 시각 품질 개선

검증된 핵심 경험 위에 지구 텍스처·대기, 행성 재질, 토성 고리, 은하 입자를 순차 추가한다. 에셋의 라이선스·용량·출처를 기록하고 전환 전에 필요한 자원을 준비한다.

완료 조건: 개선 요소가 축척 이해를 해치지 않고, 낮은 품질 설정에서도 동일한 내용을 탐색할 수 있다. 대기 셰이더나 후처리는 성능 측정 후 선택적으로 적용한다.

### 5단계 — 성능·접근성·배포

실제 데스크톱과 모바일에서 병목을 측정하고, DPR 상한·입자 수·텍스처 해상도·라벨 수를 조정한다. 정적 빌드를 GitHub Pages의 하위 경로에서도 검증한다.

완료 조건: 빌드와 미리보기가 성공하고 텍스처 경로 오류가 없으며, 키보드 탐색·애니메이션 축소·오류 안내를 확인한다. 측정 기기와 결과를 `docs/qa.md`에 남긴다. 실제 공개 배포는 배포 작업이 요청된 시점에 진행한다.

## 9. 검증과 성능 목표

테스트는 화면 구조를 그대로 복제하기보다 오류가 경험을 망치는 핵심 로직에 집중한다.

- 단위 변환: 단위 경계와 표기 반올림 확인.
- 축척 수학: `zoom`이 1 증가하면 같은 대상의 투영 크기가 1/10이 되는지 확인.
- 좌표 변환: 지구 기준점 유지, 구조별 로컬 좌표 변환, 극단 규모의 유한값 확인.
- 단계 전환: 경계에서 활성 장면과 불투명도가 의도한 범위에 있고 공백 구간이 없는지 확인.
- 데이터: 중복 ID, 끊긴 참조, 잘못된 반지름, 누락된 출처 확인.
- 수동 시각 검증: 주요 단계와 전환 중간, 세로 모바일, 화면 회전, 빠른 휠 입력, 핀치, 컨텍스트 손실·복구 확인.

잠정 성능 목표는 데스크톱 60fps, 모바일 30fps 이상이다. 보장 수치로 취급하지 않고 실제 측정 기기와 함께 평가한다. 활성 장면 수, 드로 콜, 입자 수, 텍스처 메모리, 첫 로딩 전송량을 기록해 구체적인 예산을 정한다.

움직임이 없는 상태에서는 불필요한 재렌더링을 줄이고, 장면 왕복 후 GPU 메모리가 계속 증가하지 않도록 geometry·material·texture의 소유권과 해제 시점을 관리한다.

## 10. 초기 범위에서 제외하는 작업

- 실시간 천체력, 궤도 역학, 정밀 천문 시뮬레이션.
- 자유 비행, 무제한 팬, VR/AR, 멀티플레이.
- 우주 전체를 하나의 고정 단위 3D 좌표계로 구현하는 방식.
- 모든 천체의 고해상도 모델·텍스처와 복잡한 후처리.
- 관측 자료로 검증하지 않은 절차적 은하 분포를 실제 우주 지도처럼 제시하는 것.

## 11. 후속 작업 원칙

1. 기능 변경 전 이 계획과 현재 코드를 확인한다.
2. 3D 기술 검증 → 전체 규모 연결 → 탐색 MVP → 시각 개선 순서를 지킨다.
3. 사용자 요청 없이 프레임워크·백엔드·자유 카메라를 추가하지 않는다.
4. 수치와 표현 방식의 가정을 명시하고, 검증되지 않은 정확성을 주장하지 않는다.
5. 구현 후 변경 내용, 실제 수행한 검증, 남은 제한을 간결하게 기록한다.

이 파일은 사용자가 요청한 `agent.md`라는 이름의 계획 문서다. 자동 로드되는 에이전트 지침 파일이 필요해지면 사용하는 도구의 파일명 규칙을 별도로 확인한다.
