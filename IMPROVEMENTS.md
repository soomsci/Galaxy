# 개선사항 — 시뮬레이션 정확도와 체감

현재 구현(`src/`, 795줄, 테스트 7개 통과)을 점검한 결과. 효과/비용 순으로 정렬했다.
1·3·4번은 짧게 끝나고, 2번이 실제 작업량이 있는 항목이다.

## 현재 상태 요약

`CLAUDE.md`의 초기 기획(Canvas 2D·단일 파일)과 실제 구현이 갈라져 있다. 구현은 `agent.md`의 수정 계획을 따라 **Three.js + Vite, 8단계 스테이지** 구조로 갔다.

| | 상태 |
|---|---|
| `src/core/scale.js` | `zoom = log10(화면폭 m)` 단일 상태 유지. `damp`의 프레임레이트 독립성은 테스트로 보장 |
| `src/rendering/space.js` | 행성은 실제 구, 그 이상은 절차적 포인트클라우드. `visibility(zoom, start, end)`로 크로스페이드 |
| 잘 되어 있는 것 | 지구 원점 고정, 중앙 레티클, 단계별 표현 한계 고지(`note`), NASA/ESA 출처 링크 |

계획 대비 바뀐 핵심은 "크기로 자동 필터링" → "**zoom 구간을 손으로 지정한 레이어**". 아래 문제 대부분이 여기서 나온다.

---

## 1. 빛의 여행 시간 표시 · 효과 최상 / 3줄

거리 데이터는 다 있는데 시간으로 환산해 주지 않는다. 앱의 주제가 "공간이 얼마나 큰가"인데, 사람은 미터보다 시간으로 크기를 느낀다.

- `renderUi()`의 `scale-value` 옆에 한 줄 추가: `화면 가로 폭 = 빛으로 8분 / 4.2년 / 250만 년`
- 안드로메다 단계에 한 문장: "지금 보는 빛은 250만 년 전에 출발했습니다"

별 1,000억 개라는 숫자보다 이 한 줄이 세다.

## 2. 은하 좌표계 하나로 통일 · 방향이 실제와 다름 / ~15줄

방향 벡터가 전부 손으로 찍은 값이라 서로 모순이다.

- **궁수자리 왜소은하** (`space.js:228`) `[.38, .72, -.58]` → +x 방향. 그런데 은하 중심은 `-x`(`space.js:330`). 실제로는 은하 중심 **바로 너머**(l≈6°, b≈-14°)에 있어야 하는데 하늘 반대편에 그려져 있다.
- **대마젤란운** (`space.js:229`) `[.3, -.82, .48]` → 실제 l=280°, b=-33°이면 은하 원반 **남쪽**인데 현재 z가 +라서 북쪽에 있다.

**고치는 법:** 데이터를 `{ l, b, distanceLy }`로 바꾸고, `galacticToWorld(l, b)` 함수 하나 + 은하 원반 기울기 행렬 하나를 **모든 천체가 공유**하게 한다. 그러면 위성은하·안드로메다·은하중심 라벨이 전부 자동으로 맞는다. "지구 기준"이라고 써 붙인 만큼 방향도 맞아야 설득력이 생긴다.

## 3. 가까운 별 4개, 라벨과 배치 거리 불일치 · 버그 / 1줄

`space.js:214-217`의 좌표가 정규화되지 않은 손수 값이라 라벨보다 6~10% 가깝게 그려져 있다.

| 별 | 라벨 | 실제 배치 | 오차 |
|---|---|---|---|
| 프록시마 센타우리 | 4.25 광년 | 4.00 광년 | -5.9% |
| 시리우스 | 8.6 광년 | 7.92 광년 | -7.9% |
| 엡실론 에리다니 | 10.5 광년 | 9.72 광년 | -7.4% |
| 프로키온 | 11.5 광년 | 10.38 광년 | -9.8% |

이미 있는 `positionAtDistance(distanceLy, direction)`(`space.js:83`)를 쓰면 끝. **2번을 하면 공짜로 같이 고쳐진다.**

## 4. 궁수자리 왜소은하 거리 확인 · 데이터

`universe.js` → `distanceLy: 90000`. 통상 인용값은 약 **6.5만 광년**(≈20 kpc)이다.
나머지 거리(LMC 16.2만, SMC 20만, 삼각형자리 273만, 안드로메다 250만)는 모두 맞다.

## 5. "실제 크기 모드" 토글 · ~5줄

지금은 `setObject(..., minPixels)`와 행성 마커 하한(`space.js:301`) 때문에 작은 것이 전부 부풀려져 있다. 국부은하군 단계에서 우리은하는 최소 18px, 안드로메다는 22px로 고정 — **은하 사이의 빈 공간이 실제보다 좁아 보인다.** 앱이 말하려는 것과 정반대다.

가독성 기본값은 그대로 두고, 체크박스 하나로 모든 하한을 0으로 만드는 모드를 준다. 별과 은하가 점으로 사라지는 그 순간이 사실 이 앱의 클라이맥스다.

## 6. 행성 공전 · 2줄

행성이 `phase` 고정이라 태양계가 정지 화면이다.
`angle = phase + t / orbitAU ** 1.5` (케플러 3법칙) 한 줄이면 해왕성이 수성보다 165배 느리게 도는 게 눈에 보인다. `prefers-reduced-motion` 처리는 이미 있으니 거기 얹으면 된다.

## 7. 항성 배경을 구 → 원반으로 · 1줄

`space.js:206`의 먼 별들이 균일한 구 분포라, 바로 다음 단계에 나올 은하 원반과 모순된다.
먼 성분의 z에 `* 0.15`를 곱하면 성간 단계에서 이미 원반이 암시된다.

---

## 일부러 안 하는 것

- **우주 팽창 / 적색편이** — 자릿수 체감이 목적이지 우주론 시뮬레이터가 아님
- **실제 천체력(ephemeris), 관측 카탈로그 연동** — 6번의 케플러 근사로 충분
- **행성 축 기울기·자전 주기·위성계** — 스케일 체감에 기여하지 않음

`agent.md`의 범위 판단은 그대로 유효하다.
